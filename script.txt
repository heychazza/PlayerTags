classpath     "%classpathLibs%"
              "%compileArtifactPath%"
              "%pluginArtifactPath%";
open          "server/plugins/%project.build.finalName%.%project.packaging%";

stringEncryptionExclude
              io.felux.playertags.PlayerTags;

trimExclude   *.* @*.EventHandler *(*);

exclude       io.felux.playertags. and
              io.felux.playertags.PlayerTags and

              io.felux.playertags.api. and
              io.felux.playertags.api.* and
              io.felux.playertags.api.* * and
              io.felux.playertags.api.* * (*) and

              io.felux.playertags.storage.*.*PlayerData and
              io.felux.playertags.storage.*.*PlayerData * and
              io.felux.playertags.storage.*.*PlayerData * (*)
              ;

obfuscate   obfuscateFlow=aggressive
            encryptStringLiterals=enhanced
            mixedCaseClassNames=true
            aggressiveMethodRenaming=true
            hideFieldNames=true
            uniqueClassNames=true
            randomize=true
            localVariables=delete
            lineNumbers=scramble
            autoReflectionHandling=normal
            obfuscateReferences=none
            obfuscateReferenceStructures=inSpecialClass
            lineNumbers=scramble;

saveAll     "server/plugins/%project.build.finalName%.%project.packaging%";